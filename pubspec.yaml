name: kisan
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  image: ^4.0.19  # Correctly added outside the flutter block
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  image_picker:  ^1.1.2
  tflite_flutter: ^0.11.0
  file_picker: ^8.1.4
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/models/model_unquant.tflite
    - assets/models/labels.txt
